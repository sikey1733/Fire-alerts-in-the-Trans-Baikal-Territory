name: 🔥 Fire Risk Daily Pipeline

on:
  schedule:
    - cron: "0 11 * * *"  # каждый день в 14:00 по Москве (UTC+3)
  workflow_dispatch:

jobs:
  run-r-script:
    runs-on: ubuntu-latest

    env:
      TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      VIIRS_KEY: ${{ secrets.VIIRS_KEY }}
      CDS_API_KEY: ${{ secrets.CDS_API_KEY }}
      CDS_USER_ID: ${{ secrets.CDS_USER_ID }}

    steps:
      - name: 🔽 Клонирование репозитория
        uses: actions/checkout@v4

      - name: 🛠️ Установка R
        uses: r-lib/actions/setup-r@v2

      - name: 📦 Кэширование библиотек R
        uses: actions/cache@v3
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ hashFiles('**/*.R', '**/*.Rmd', '**/DESCRIPTION') }}
          restore-keys: |
            ${{ runner.os }}-r-

      - name: 📦 Установка зависимостей
        run: |
          Rscript -e 'install.packages(c(
            "ecmwfr", "tidyverse", "stars", "openair", "httr", "lubridate", 
            "osmdata", "sf", "leaflet", "geosphere", "mapview", "htmlwidgets", 
            "webshot", "ggplot2", "rmarkdown", "curl", "xml2"
          ), repos = "https://cloud.r-project.org/")'
          Rscript -e 'webshot::install_phantomjs()'

      - name: 🚀 Запуск основного скрипта
        run: |
          Rscript -e 'source("pipeline.R"); main()'

      - name: 📂 Просмотр содержимого папки output/
        run: ls -lh output/

      - name: 📤 Загрузка артефактов (output/)
        uses: actions/upload-artifact@v4
        with:
          name: output-plots
          path: output/
          if-no-files-found: warn
